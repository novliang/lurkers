# mysql connect configuration
mysql:
  username: root
  password: 'mytest'
  path: '127.0.0.1:3306'
  db-name: 'giant'
  config: 'charset=utf8&parseTime=True&loc=Local'
  max-idle-conns: 10
  max-open-conns: 10
  log-mode: false

# jwt configuration
jwt:
  signing-key: 'qmPlus'
  default-expire: 5184000

# redis configuration
redis:
  addr: '127.0.0.1:6379'
  password: ''
  db: 0

# system configuration
system:
  addr: 8888

# logger configuration
log:
  prefix: '[GIANT]'

# captcha configuration
captcha:
  sign: "曜辉科技"
  expire: 120